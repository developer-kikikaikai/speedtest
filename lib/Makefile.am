#load common setting
include $(top_srcdir)/am.conf

noinst_LTLIBRARIES = libtimelog.la
#
#lib_LTLIBRARIES = libtimelog.la

libtimelog_la_SOURCES = timetestlog.c
#for statik library
#libtimelog_la_LDFLAGS=-module -shared -export-dynamic -static -shared -rpath $(libdir)
